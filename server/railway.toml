# Railway: servis kök dizini "server" olmalı.
# builder = NIXPACKS iken Nixpacks nodejs_22 (22.11) Prisma 7.6+ ile kırılıyordu; Dockerfile Node 20 kullanır.
[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = "node dist/index.js"
