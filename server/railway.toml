# Railway: Root Directory = server olarak ayarla (veya bu dosyayı kullan)
[build]
builder = "NIXPACKS"
# Nixpacks zaten install aşamasında npm ci çalıştırır; burada yalnızca build yeterli.
buildCommand = "npm run build"

[deploy]
startCommand = "node dist/index.js"
